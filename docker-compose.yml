version: "3.9"

services:
  anomaly-api:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: anomaly_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: always
